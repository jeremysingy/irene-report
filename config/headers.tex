%------------------------------------------------
% Headers and footers configuration
%------------------------------------------------

% Custom header height
%\setlength{\headheight}{22pt}

% Show headers also in plain pages
%\fancypagestyle{plain}{}

% Remove page number in plain pages
\fancypagestyle{plain}{\fancyhf{}\renewcommand{\headrulewidth}{0pt}\renewcommand{\footrulewidth}{0pt}}

\pagestyle{fancy}
\fancyhf{}

% Add a rule also for footer
\renewcommand{\footrulewidth}{0.4pt}

%\let\oldref\ref
%\renewcommand{\ref}[1]{\hypersetup{linkcolor=black}\oldref{#1}\hypersetup{linkcolor=blue}}

\renewcommand{\chaptermark}[1]{\markboth{\bsc{\chaptername~\thechapter{} :} #1}{}}
\renewcommand{\sectionmark}[1]{\markright{\thesection{} #1}}

% Odd page (recto, right-hand) and even page (verso, left-hand)
\fancyhead[RE,LO]{\doctitle}
\fancyhead[LE,RO]{\rightmark}
\fancyfoot[RE,LO]{\authorname}
\fancyfoot[LE,RO]{\thepage{} of \pageref*{LastPage}}

%\fancyfoot[C]{\leftmark}

% Display header of the TOC to lowercase
\fancyhead[RO]{\nouppercase\rightmark}

% Customized current section style (using lowercase, etc.)
%\renewcommand{\chaptermark}[1]{\markboth{\bsc{\chaptername~\thechapter{} :} #1}{}}
%\renewcommand{\chaptermark}[1]{\markboth{#1}{}} % Lower case chapter
%\renewcommand{\sectionmark}[1]{\markright{#1}}  % Lower case section

%\renewcommand{\chaptermark}[1]{\markboth{\bsc{\chaptername~\thechapter{} :} #1}{}}
%\renewcommand{\sectionmark}[1]{\markright{\thesection{} #1}}

\renewcommand{\sectionmark}[1]{\markright{\thesection{} #1}}
